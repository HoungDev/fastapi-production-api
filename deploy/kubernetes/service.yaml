apiVersion: v1
kind: Service
metadata:
  name: fastapi-production-api
  labels:
    app.kubernetes.io/name: fastapi-production-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fastapi-production-api
  ports:
    - name: http
      port: 80
      targetPort: http
